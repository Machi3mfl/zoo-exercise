{
  "name": "zoo-exercise",
  "version": "1.0.0",
  "description": "Zoo Exercise",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "NODE_ENV=test jest --testTimeout=200000 --verbose --runInBand",
    "test:watch": "npm run test -- --watchAll",
    "test:coverage": "NODE_ENV=test jest --testTimeout=200000 --verbose --runInBand --coverage"
  },
  "author": "Maximiliano Ibarra",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.3.1"
  }
}
